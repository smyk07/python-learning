# lambda function => functions written in one line using lambda keyword
# lambda parameters:expression

double = lambda x : x*2
multiply = lambda x, y : x*y 

print(double(2)) 
print(multiply(5, 7))

